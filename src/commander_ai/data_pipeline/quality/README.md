# Quality

Stable quality finding codes, gates and reports.
Finding codes use the namespaces `parse.*`, `integrity.*`, `resolution.*`,
`legality.*`, and `quality.*`. A failed row is quarantined with its original values
and exact raw locator; no quarantine or audit record can be relabeled as curated.
