# Local Data

Dieser Ordner ist nicht die Git-Datenbank. Nur dieses README und gegebenenfalls leere Verzeichnisstruktur werden committed.

Raw, normalized, curated, indexes und cache folgen `docs/03-data/storage-layout.md`.
